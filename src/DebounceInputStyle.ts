import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  searchContainer: { width: '80%', alignSelf: 'center', margin: 6, paddingHorizontal: 16, borderRadius: 15, borderColor: 'black', borderWidth: 1 }
})